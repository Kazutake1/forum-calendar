# 更新履歴

## Ver.9.3.6 — iOSの手動JSON本文読み込み障害対策（2026-09-20）
- fetch成功後のJSON本文取得失敗も含めて最大2回試行し、失敗時は保存済みデータを検査して利用。
- Service Workerで応答本文を一度だけ読み込んでから表示・保存し、クローンされたストリームの競合を回避。
- JSON本文の読み込み失敗とキャッシュ保存失敗を模擬した回帰テストを追加。イベントデータは変更しない。

## Ver.9.3.5 — 手動イベントの取得障害対策（2026-09-20）
- 公開中の手動イベント4件と自動イベント80件による統合テストを恒久追加。
- 手動データをService Workerへ事前保存し、通信障害時は保存済みデータに切り替えて警告を表示。
- 取得・JSON解析・統合・表示の失敗を区別し、原因を画面と開発者ログに表示。
- 正常な通信結果はキャッシュ保存失敗に妨げられず表示する。既存イベントデータや自動更新処理は変更なし。

## Ver.9.3.4 — イベント情報の安全な統合（2026-09-20）
- 既存イベントJSONが欠落・破損・空の場合は自動更新を停止し、元データを保護。
- 同じ公演と確認できる場合のみ統合し、異なる開演時間は分離。開場と開演を区別。
- 項目単位の出典優先順位は確認済みのイベント公式サイト > 稲沢市催事予定表 > X。単なるURLは確認済みとみなさない。
- 食い違いは根拠付きの明示的照合を必要とし、テストを追加。既存イベントデータは変更なし。

## Ver.9.3.3 — フロントエンド安全性修正（2026-09-20）
- イベント一覧の開催時間をHTMLエスケープして表示し、外部イベントデータがHTMLとして解釈されないよう修正。
- Service Workerの一括登録解除を廃止し、当アプリのスコープに対する通常の登録・更新のみ実行。
- 開催時間が未設定のイベントでも一覧の並べ替えが例外で停止しないよう修正。
- キャッシュ識別子を更新。イベントデータ、自動更新処理、画面デザインは変更なし。

## Ver.9.3.2 運用改善 — 手動イベントのアーカイブと月次重複点検（2026-09-20）
- `manage_manual_events.py` と月次GitHub Actionsを追加。毎月1日10:10（日本時間）に、開催日から90日を**過ぎた**手動イベントを `archive/manual_events_開催年.json` へ移動する。原データ・出典は保持し、再実行でも二重保存しない。
- 公開画面が読む `manual_events.json` から対象イベントを除外する。`events.json`、自動更新プログラム、表示デザインは変更しない。
- `reports/manual-event-review.md` を毎月生成。同日同会場・同日同名・近接日の同名イベントを「確認候補」として提示するだけで、自動削除・自動統合はしない。
- ID重複・日付不正・アーカイブ年の不一致があれば安全のため処理を停止する。処理と回帰テスト、GitHub Pagesの再ビルド要求をワークフローに追加。
- GitHub Actionsの `GITHUB_TOKEN` によるコミットは通常のGitHub Pagesビルドを誘発しないため、保存後にPagesビルドAPIを明示的に要求し、失敗時にはワークフローを失敗扱いにする。
- 既存の自動取得データに残る過去イベントは、本機能の整理対象外。手動登録時も出典と重複を確認する。

## Ver.9.3.2 — 手動イベントの表示基盤
- 自動更新データ `events.json` はそのまま維持し、手動登録は別ファイル `manual_events.json` に保存する。
- 手動イベントを表示時に統合し、日時・名称・会場を用いて重複を照合する。名称・日時の不一致は明示的な `match` による確認を必要とする。
- 出典URL付きで稲沢市公式サイト確認済みの情報を優先する。出典不明の自動イベントは既存情報を保持し、Xの内容で無断上書きしない。
- 手動登録データの取得・検証に失敗した場合は画面に警告を表示する。
- 取り込み画面は設けず、イベント一覧は登録内容を確認した後にGitHub連携で更新する。

### 手動イベントの必須フィールド
`id`, `date` (YYYY-MM-DD), `title`, `hall`, `source_type` (`x` / `city_official` / `other`), `source_url` (HTTPS URL)。任意: `time`, `price`, `venues`, `source_verified`, `match` (`date`, `title`, 必要なら `hall`, `time`)。`source_type=city_official` は稲沢市公式ドメインのURLと `source_verified=true` の両方が必要。

**注意:** 曖昧な重複候補や情報の食い違いは、登録前に人が確認する。公式サイトへの一般的なリンクがあるだけでは個別イベントを「公式確認済み」と扱わない。
